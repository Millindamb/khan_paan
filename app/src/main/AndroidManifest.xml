<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gharkakhana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GharKaKhana">
        <activity
            android:name=".recentOrderItems"
            android:exported="false" />
        <activity
            android:name=".DetailsActivity"
            android:exported="false" />
        <activity
            android:name=".PayOutActivity"
            android:exported="false" /> <!-- Splash Screen (First screen) -->
        <activity
            android:name=".Splash_Screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Start Activity -->
        <activity
            android:name=".StartActivity"
            android:exported="false" /> <!-- Login Screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" /> <!-- Sign Up Screen -->
        <activity
            android:name=".SigninActivity"
            android:exported="false" /> <!-- Choose Location Screen -->
        <activity
            android:name=".ChooseLocationActivity"
            android:exported="false" /> <!-- Main App Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>